package com.bbva.[(${atiSettings.getUuaa().toLowerCase()})].apisetup;

import static org.junit.Assert.assertNotNull;

import com.bbva.[(${atiSettings.getUuaa().toLowerCase()})].[(${atiSettings.getServiceName().toLowerCase()})].apisetup.ConfigBatchconfig;
import org.junit.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.annotation.DirtiesContext;
import org.springframework.test.context.TestPropertySource;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.bbva.gkxj.apirestgen.batchconfig.client.jaxrs.rest.impl.RestHandlerBatchconfig;
import com.bbva.kltt.apirest.generator.lib.commons.client.request.NovaRequestConfig;
import com.bbva.kltt.apirest.generator.lib.commons.metadata.NovaMetadata;

@SpringBootTest(classes = {ConfigBatchconfig.class})
@DirtiesContext
@ExtendWith(MockitoExtension.class)
@TestPropertySource(properties = {
    "microgateway.hostport.Batchconfig=localhost:24000",
    "microgateway.timeout.Batchconfig=10000",
    "release.header.Batchconfig=seguridad"
})
@RunWith(SpringJUnit4ClassRunner.class)
public class ConfigBatchconfigTest {

    @MockBean
    private RestHandlerBatchconfig restHandlerBatchconfig;

    @InjectMocks
    @Autowired
    private ConfigBatchconfig configBatchconfig;

    @Test
    public void testGetRestHandlerBatchconfig() {
        RestHandlerBatchconfig restHandler = configBatchconfig.getRestHandlerBatchconfig();
        assertNotNull("RestHandlerBatchconfig should not be null", restHandler);
    }

    @Test
    public void testGetNovaMetadata() {
        NovaMetadata novaMetadata = configBatchconfig.getNovaMetadata();
        assertNotNull("NovaMetadata should not be null", novaMetadata);
        assertNotNull("NovaImplicitHeadersOutput should not be null", novaMetadata.getNovaImplicitHeadersOutput());
    }

    @Test
    public void testGetConfigRequestSchema() {
        NovaRequestConfig requestConfig = configBatchconfig.getConfigRequestSchema();
        assertNotNull("NovaRequestConfig should not be null", requestConfig);
    }
}