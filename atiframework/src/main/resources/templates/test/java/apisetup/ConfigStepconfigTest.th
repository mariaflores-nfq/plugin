package com.bbva.[(${atiSettings.getUuaa().toLowerCase()})].apisetup;

import static org.junit.Assert.assertNotNull;

import com.bbva.[(${atiSettings.getUuaa().toLowerCase()})].[(${atiSettings.getServiceName().toLowerCase()})].apisetup.ConfigStepconfig;
import org.junit.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.annotation.DirtiesContext;
import org.springframework.test.context.TestPropertySource;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.bbva.gkxj.apirestgen.stepconfig.client.jaxrs.rest.impl.RestHandlerStepconfig;
import com.bbva.kltt.apirest.generator.lib.commons.client.request.NovaRequestConfig;
import com.bbva.kltt.apirest.generator.lib.commons.metadata.NovaMetadata;

@SpringBootTest(classes = {ConfigStepconfig.class})
@DirtiesContext
@ExtendWith(MockitoExtension.class)
@TestPropertySource(properties = {
    "microgateway.hostport.Stepconfig=localhost:24000",
    "microgateway.timeout.Stepconfig=10000",
    "release.header.Stepconfig=seguridad"
})
@RunWith(SpringJUnit4ClassRunner.class)
public class ConfigStepconfigTest {

    @MockBean
    private RestHandlerStepconfig restHandlerStepconfig;

    @InjectMocks
    @Autowired
    private ConfigStepconfig configStepconfig;

    @Test
    public void testGetRestHandlerStepconfig() {
        RestHandlerStepconfig restHandler = configStepconfig.getRestHandlerStepconfig();
        assertNotNull("RestHandlerStepconfig should not be null", restHandler);
    }

    @Test
    public void testGetNovaMetadata() {
        NovaMetadata novaMetadata = configStepconfig.getNovaMetadata();
        assertNotNull("NovaMetadata should not be null", novaMetadata);
        assertNotNull("NovaImplicitHeadersOutput should not be null", novaMetadata.getNovaImplicitHeadersOutput());
    }

    @Test
    public void testGetConfigRequestSchema() {
        NovaRequestConfig requestConfig = configStepconfig.getConfigRequestSchema();
        assertNotNull("NovaRequestConfig should not be null", requestConfig);
    }
}