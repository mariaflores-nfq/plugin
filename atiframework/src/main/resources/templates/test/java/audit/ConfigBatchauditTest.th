package com.bbva.[(${atiSettings.getUuaa().toLowerCase()})].audit;

import static org.junit.Assert.assertNotNull;

import com.bbva.[(${atiSettings.getUuaa().toLowerCase()})].[(${atiSettings.getServiceName().toLowerCase()})].audit.ConfigBatchaudit;
import org.junit.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.annotation.DirtiesContext;
import org.springframework.test.context.TestPropertySource;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.bbva.gkxj.apirestgen.batchaudit.client.jaxrs.rest.impl.RestHandlerBatchaudit;
import com.bbva.kltt.apirest.generator.lib.commons.metadata.NovaMetadata;
import com.bbva.kltt.apirest.generator.lib.commons.client.request.NovaRequestConfig ;

@SpringBootTest(classes = {ConfigBatchaudit.class})
@DirtiesContext
@ExtendWith(MockitoExtension.class)
@TestPropertySource(properties = "spring.cloud.config.enabled=false")
@RunWith(SpringJUnit4ClassRunner.class)
public class ConfigBatchauditTest {

    @MockBean
    private RestHandlerBatchaudit restHandlerBatchaudit;

    @InjectMocks
    @Autowired
    private ConfigBatchaudit configBatchaudit;

    @Test
    public void testGetRestHandlerBatchaudit() {
    	  RestHandlerBatchaudit restHandler = configBatchaudit.getRestHandlerBatchaudit();
          assertNotNull("RestHandlerBatchaudit should not be null", restHandler);
    }

    @Test
    public void testGetNovaMetadata() {
        NovaMetadata novaMetadata = configBatchaudit.getNovaMetadata();
        assertNotNull("NovaMetadata should not be null", novaMetadata);
        assertNotNull("NovaImplicitHeadersOutput should not be null", novaMetadata.getNovaImplicitHeadersOutput());
    }

    @Test
    public void testGetConfigRequestSchema() {
        NovaRequestConfig requestConfig = configBatchaudit.getConfigRequestSchema();
        assertNotNull("NovaRequestConfig should not be null", requestConfig);
    }
}